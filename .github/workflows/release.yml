name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  macos-dmg:
    name: Build unsigned macOS DMG
    runs-on: macos-latest
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: "false"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.12.0"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck, lint, format, and test
        run: pnpm check

      - name: Build unsigned DMG and update ZIP
        run: pnpm desktop:dist

      - name: Generate checksums
        working-directory: apps/desktop/dist
        # SHA256SUMS.txt is the human-facing checksum list and the source the
        # update manifest reads hashes from below.
        run: shasum -a 256 *.dmg *.zip > SHA256SUMS.txt

      - name: Build update manifest
        # latest.json is what the in-app updater fetches (via the CDN-served
        # releases/latest/download redirect) instead of the rate-limited GitHub
        # API. It pulls hashes from SHA256SUMS.txt and notes from CHANGELOG.md.
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
        run: |
          node apps/desktop/scripts/build-update-manifest.mjs \
            apps/desktop/dist \
            CHANGELOG.md \
            apps/desktop/dist/latest.json
          echo "----- latest.json -----"
          cat apps/desktop/dist/latest.json

      - name: List release artifacts
        run: ls -lh apps/desktop/dist

      - name: Build release notes from CHANGELOG
        # The body is the matching CHANGELOG section verbatim (no install
        # boilerplate, so the in-app updater can aggregate sections cleanly).
        # Fail loudly when the section is missing — a release must have notes.
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          awk -v ver="$VERSION" '
            $0 ~ "^## \\[" ver "\\]" { capture = 1; next }
            capture && /^## \[/ { exit }
            capture { print }
          ' CHANGELOG.md | sed -e '/./,$!d' > RELEASE_NOTES.md
          if [ ! -s RELEASE_NOTES.md ]; then
            echo "::error::No CHANGELOG.md section found for ${VERSION}. Add one before tagging." >&2
            exit 1
          fi
          echo "----- release notes -----"
          cat RELEASE_NOTES.md

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            apps/desktop/dist/*.dmg \
            apps/desktop/dist/*.zip \
            apps/desktop/dist/SHA256SUMS.txt \
            apps/desktop/dist/latest.json \
            --verify-tag \
            --title "Yui ${GITHUB_REF_NAME}" \
            --notes-file RELEASE_NOTES.md
